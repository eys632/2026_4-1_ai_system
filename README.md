# 2026 4-1 AI System

- `ver2/`: 기존 레포에 있던 내용(그대로 폴더로 이동)
- `ver3/`: 0415_v3에서 만든 RAG 데모 버전

## ver3 실행 메모
- PDF 원문은 레포에 포함하지 않았습니다(저작권/용량 이슈). 실행 시 로컬에 PDF를 두고 경로를 인자로 넘겨 빌드/질의를 수행하세요.
- 예시(레포 루트에서):
  - `cd ver3`
  - `python -m src.raglab.web_demo --help`
